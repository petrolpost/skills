# Role:

> 复制本文件为 `.petrelpost/docs/collaboration/roles/<role-slug>.md`，填写后删除本行提示。
> 只写这个角色专属的内容；角色之间的关系一律写在 `.petrelpost/docs/collaboration/roles/overview.md`，不要在这里重复。
> Task 字段规范见 `.petrelpost/docs/collaboration/role-tasks/TaskTemplate.md`，不要在这里重复。

## 定位

{一句话：由谁 / 什么承担，在协作闭环中的位置}

## 使命（Mission）

{这个角色要优化的目标是什么——不是任务清单，是它存在的理由}

## 职责（能做什么）

## 决策权限边界（不能做什么 / 需上报给谁）

- 不能：
- 需上报：{哪个角色}

## 输入

- 依赖谁的产出 / 从哪里获取信息

## 输出 / 交付物格式

## 交接协议

- 完成后交给谁、触发条件是什么
- 对应 `.petrelpost/docs/collaboration/roles/overview.md` 交接矩阵中的哪一行

## 何时算完成（Acceptance）

# Role:

> 复制本文件为 `docs/collaboration/roles/<role-slug>.md`，填写后删除本行提示。
> 只写这个角色专属的内容；角色之间的关系一律写在 `docs/collaboration/roles/overview.md`，不要在这里重复。
> Task 字段规范见 `docs/tasks/TaskTemplate.md`，不要在这里重复。

## 定位

{一句话：由谁 / 什么承担，在协作闭环中的位置}

## 使命（Mission）

{这个角色要优化的目标是什么——不是任务清单，是它存在的理由}

## 职责（能做什么）

## 决策权限边界（不能做什么 / 需上报给谁）

- 不能：
- 需上报：{哪个角色}

## 输入

- 依赖谁的产出 / 从哪里获取信息

## 输出 / 交付物格式

## 交接协议

- 完成后交给谁、触发条件是什么
- 对应 `docs/collaboration/roles/overview.md` 交接矩阵中的哪一行

## 何时算完成（Acceptance）
