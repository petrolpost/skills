# Roles Overview — 角色综述与协作关系

本文件描述角色**之间**的关系。单个角色的职责、权限、输入输出见 `docs/collaboration/roles/` 下对应文件。

Task 文档的字段规范见 `docs/tasks/TaskTemplate.md`，本文件不重复列出。

---

## 1. 角色一览

| 角色 | 承担者 | 一句话使命 | 详情 |
|---|---|---|---|
| {角色 A} | {人 / Agent} | {一句话} | `docs/collaboration/roles/{slug}.md` |
| {角色 B} | {人 / Agent} | {一句话} | `docs/collaboration/roles/{slug}.md` |

---

## 2. 协作闭环

```text
{起点角色}
    ↓
{下一步}
    ↓
{...}
    ↓
回到 {起点角色}
```

---

## 3. 交接矩阵（Handoff Matrix）

| 从 → 到 | 触发条件 | 交付物 / 格式 | 谁能打回 |
|---|---|---|---|
| {角色 A} → {角色 B} | {什么情况下发生} | {文件/格式} | {谁、什么条件下可以打回} |

---

## 4. 升级与分歧裁决

- {角色 X} 与 {角色 Y} 之间的分歧：由谁裁定，记录在哪。
- 涉及项目方向 / 边界的分歧：最终决策角色拥有裁决权；若裁决改变了长期方向，按项目决策记录规则形成新记录。

---

## 5. 边界声明

{这套协作模式本身是否是研究/观察对象，还是仅作为工作方式——按项目实际情况填写}

本文件不描述具体业务方法论，只描述角色间协作方式。
